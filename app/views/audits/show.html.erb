<% content_for :title, "Audit — #{@project.display_name}" %>
<%= render "shared/project_header" %>
<%= render "shared/project_nav", project: @project, destination: params[:destination] %>

<%= render layout: "ui/card", locals: { title: "kamal audit" } do %>
  <%= render "ui/terminal", content: @audit_output %>
<% end %>
